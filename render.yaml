services:
  - type: web
    name: urbansaathi-backend
    env: node
    plan: free
    buildCommand: "cd backend && npm install && npm run build"
    startCommand: "cd backend && npm run start:prod"
    envVars:
      - key: PORT
        value: "4000"
      - key: NODE_ENV
        value: "production"
